// Phase 4 占位：仅用于让 SongList 在 Phase 5 之前能编译通过
export default () => {
  const likeMusic = async () => {
    // no-op
  }
  const deleteSongHandler = async () => {
    // no-op
  }
  return {
    likeMusic,
    deleteSongHandler
  }
}